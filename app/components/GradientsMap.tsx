export const GradientsMap = (gradient: string[]) => {
    return (<>
        {gradient.map((g) => {
            return (
                <>
                    hi
                </>
            )
        })}

    </>)
}

export default GradientsMap;

